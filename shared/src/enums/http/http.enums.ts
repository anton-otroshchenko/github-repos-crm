const HttpCode = {
	UNAUTHORIZED: 401,
};

const HttpMessage = {
	UNAUTHORIZED: "Unauthorized",
	INVALID_TOKEN: "Invalid token",
	TOKEN_EXPIRED: "Token expired",
	USER_NOT_FOUND: "User not found",
};

export { HttpMessage, HttpCode };
